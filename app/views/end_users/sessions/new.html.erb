<main>
  <div class="container">
    <div class="row">
      <div class="user_form col-md-8 offset-md-2">
        <h3 class="mb-3">会員の方はこちらからログイン</h3>
        <div class="col-md-10 offset-md-1 mb-5">
          <div>
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="form-group">
                <%= f.label :email, class: "control-label sr-only" %>
                <%= f.email_field :email, class: "form-control", placeholder:"メールアドレス" %>
              </div>
              <div class="form-group">
                <%= f.label :password, class: "control-label sr-only" %>
                <%= f.password_field :password, autocomplete: :off, class:"form-control", placeholder:"パスワード" %>
              </div>
              <% if devise_mapping.rememberable? %>
                <div class="field">
                  <%= f.check_box :remember_me %>
                  <%= f.label :remember_me %>
                </div>
              <% end %>
              <div class="actions">
                <%= f.submit "ログイン", class:"btn btn-lg btn-info col-md-5" %>
                <%= link_to "=> パスワードを忘れた方はこちら", "", class: "offset-md-1" %>
              </div>
            <% end %>
          </div>
        </div>
        <h3 class="mb-3">登録がお済み出ない方</h3>
        <div class="offset-md-1">
          <%= link_to "こちら", new_registration_path(resource_name) %> から新規登録を行ってください。
        </div>
      </div>
    </div>
  </div>
</main>
